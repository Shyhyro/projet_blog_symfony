{% extends "base.html.twig" %}

{% block title %}Home{% endblock %}
{% block body %}
    <div class="row mb-2">
        <div><h2>{{ post.title }}</h2> by {{ post.userFk}} </div>
        <div>{{ post.content }}</div>
    </div>

{% endblock %}